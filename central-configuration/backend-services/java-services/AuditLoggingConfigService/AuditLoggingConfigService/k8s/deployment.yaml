apiVersion: apps/v1
kind: Deployment
metadata:
  name: AuditLoggingConfigService
  labels:
    app: AuditLoggingConfigService
spec:
  replicas: 3
  selector:
    matchLabels:
      app: AuditLoggingConfigService
  template:
    metadata:
      labels:
        app: AuditLoggingConfigService
    spec:
      containers:
      - name: AuditLoggingConfigService
        image: AuditLoggingConfigService:latest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: DB_HOST
          value: "postgres-service"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 5
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1024Mi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: AuditLoggingConfigService-service
spec:
  selector:
    app: AuditLoggingConfigService
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
