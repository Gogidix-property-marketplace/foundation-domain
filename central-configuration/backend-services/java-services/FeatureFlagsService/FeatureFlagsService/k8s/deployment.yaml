apiVersion: apps/v1
kind: Deployment
metadata:
  name: FeatureFlagsService
  labels:
    app: FeatureFlagsService
spec:
  replicas: 3
  selector:
    matchLabels:
      app: FeatureFlagsService
  template:
    metadata:
      labels:
        app: FeatureFlagsService
    spec:
      containers:
      - name: FeatureFlagsService
        image: FeatureFlagsService:latest
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: DB_HOST
          value: "postgres-service"
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          initialDelaySeconds: 60
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /actuator/health/readiness
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 5
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1024Mi"
            cpu: "1000m"
---
apiVersion: v1
kind: Service
metadata:
  name: FeatureFlagsService-service
spec:
  selector:
    app: FeatureFlagsService
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
