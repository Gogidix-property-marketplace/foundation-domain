# data-quality-service

Production-ready Node.js microservice

## 🚀 Features

- **Production-ready architecture** with comprehensive error handling
- **Security-first approach** with Helmet, CORS, rate limiting, and input validation
- **Enterprise logging** with Winston and daily log rotation
- **API documentation** with Swagger/OpenAPI 3.0
- **Health checks** and monitoring with Prometheus metrics
- **Database integration** with MongoDB and Mongoose
- **Caching support** with Redis
- **Docker containerization** with multi-stage builds
- **Testing framework** with Jest and SuperTest
- **Code quality** with ESLint and Prettier

## 📋 Prerequisites

- Node.js >= 18.0.0
- npm >= 8.0.0
- MongoDB >= 5.0
- Redis >= 6.0 (optional)
- Docker & Docker Compose (for containerization)

## 🛠️ Installation

### Local Development

```bash
# Clone the repository
git clone <repository-url>
cd data-quality-service

# Install dependencies
npm install

# Copy environment configuration
cp .env.example .env

# Update .env with your configuration
```

### Docker Development

```bash
# Build and run with Docker Compose
docker-compose up -d

# View logs
docker-compose logs -f data-quality-service

# Stop services
docker-compose down
```

## 🔧 Configuration

Environment variables in `.env`:

```env
PORT=3000
NODE_ENV=development
MONGODB_URI=mongodb://localhost:27017/data-quality-service
REDIS_URL=redis://localhost:6379
JWT_SECRET=your-super-secret-jwt-key
```

## 🚀 Running the Application

### Development Mode

```bash
npm run dev
```

### Production Mode

```bash
npm run build
npm start
```

### Docker Production

```bash
docker build -t data-quality-service .
docker run -p 3000:3000 data-quality-service
```

## 📚 API Documentation

Once running, visit:
- **API Documentation**: http://localhost:3000/api-docs
- **Health Check**: http://localhost:3000/health
- **Metrics**: http://localhost:3000/metrics

## 🧪 Testing

```bash
# Run all tests
npm test

# Run tests in watch mode
npm run test:watch

# Run tests with coverage
npm run test:coverage

# Run end-to-end tests
npm run test:e2e
```

## 🔍 Code Quality

```bash
# Lint code
npm run lint

# Fix linting issues
npm run lint:fix

# Format code
npm run format
```

## 📊 Monitoring

### Health Endpoints

- `/health` - Basic health check
- `/health/ready` - Readiness probe (database, redis)
- `/health/live` - Liveness probe

### Metrics

Prometheus metrics are available at `/metrics` with:
- HTTP request duration and count
- Node.js process metrics
- Custom application metrics

### Logging

Structured JSON logs with Winston:
- Daily log rotation
- Separate error logs
- Request/response logging
- Performance tracking

## 🐳 Docker Features

- **Multi-stage builds** for optimal image size
- **Non-root user** for security
- **Health checks** with automated monitoring
- **Graceful shutdown** handling
- **Volume management** for persistent data

## ☸️ Kubernetes

Deploy with Kubernetes manifests in `k8s/`:

```bash
kubectl apply -f k8s/
```

Includes:
- Deployments with health checks
- Services with load balancing
- ConfigMaps and Secrets
- Horizontal Pod Autoscaling
- Network policies

## 🔒 Security Features

- **Helmet.js** for security headers
- **CORS** configuration
- **Rate limiting** with express-rate-limit
- **Input validation** with Joi
- **JWT authentication** support
- **Password hashing** with bcrypt
- **SQL injection protection** with Mongoose

## 🤝 Contributing

1. Fork the repository
2. Create a feature branch
3. Make your changes
4. Add tests for new functionality
5. Ensure all tests pass
6. Submit a pull request

## 📄 License

This project is licensed under the MIT License.

## 🆘 Support

For support:
- Create an issue on GitHub
- Email: support@gogidix.com

---

**Generated by Gogidix Node.js CLI - Production-Ready Microservice Generator**
